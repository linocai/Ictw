# 2026-09-20 Build 50 通知可见性快修

后续：本修复已并入 Build 51，未安装的 Build 50 包已在新版签名验证后回收；验证摘要保留，现行交付见 `2026-09-20-build51-checker-fix.md`。

## 范围与结果

- 本轮目标是让用户在前端看到阻碍发生的环节及完整原因。起点为 `d731880` 加已有未提交 Build 49 换装/设置修复与排查记录；保留此前改动，没有暂存、提交或推送。
- 后端已有安全的具体错误原因，前端此前以通用失败横幅覆盖它，Mac 通知另有 2.6 秒消失计时且只显示三行。
- 双端错误通知取消自动消失，增加可滚动、可选择文本的完整详情。章节失败横幅保留具体环节与原因；检查被拒不再误称“没能开始写”。
- 设置增加本次 App 会话最近 30 条通知。关闭提示不删除记录；恢复服务器当前失败任务时静默加入记录，按任务去重。记录不跨重启持久化，章节当前失败原因仍可由任务状态恢复。
- Mac 设置等面板内也显示通知，防止被模态面板遮住。原因使用现有错误展示器和公开原因字段，不引入失败稿逐字引证。
- 本轮未改变 Checker 判断、正文提升条件、生产服务或真实数据；上下文缺失问题仍待修复，诊断见 `2026-09-20-checker-diagnosis.md`。

## 验证与交付

- 客户端状态门禁通过，覆盖错误驻留、关闭后保留历史、恢复时静默去重、30 条边界、检查失败环节、完整原因及不展示失败稿引证。
- iOS Debug 模拟器 target 构建通过；macOS Debug 构建通过，随后补齐面板内通知与关闭按钮，最终 macOS Release 双架构构建通过。
- Mac Release 为 `v2.1.0(50)`，arm64/x86_64，Developer ID 与 hardened runtime。原产物及 ditto 复制后的产物均通过 codesign deep/strict 验证。
- 安装包：`/Users/linotsai/Lino/app_builds/ICTW-v2.1.0-build50-macOS.app`。
- 验证、可执行文件哈希与清理清单：`/Users/linotsai/Lino/app_builds/ICTW-v2.1.0-build50-validation.json`。
- 尚未换装、创建 tag 或更新公开 Release；当前本机安装仍为 Build 49。没有运行真实模型请求或做真机界面验收。
- Apple 支持检查为只读；`pending_devices` 包含 `iPhone18,4`，原因是当前开发服务不可用，需连接/解锁后在 Xcode 准备。未修改配对或清理设备缓存。

## 资源收尾

- 本轮日志集中在 `/tmp/ictw-build50-notifications-20260920`。确认所有测试/构建结束、日志包含成功结果且 lsof 无占用后，生成精确 SHA-256 清单并逐文件复核删除。
- 六个临时文件共 263710 字节，清理后该目录不存在，剩余 0 字节；系统临时目录中客户端状态测试可执行文件无残留。
- 保留 Build 50 安装包和验证摘要供换装；共享 Xcode 构建缓存供后续构建复用，既有安装包和回退备份未动。本轮未创建远端产物。
