# 2026-09-20 写作阻断只读排查

后续：已在本地 Build 51 配套 Backend 快修中补齐上下文并验证，尚未部署；见 [快修记录](2026-09-20-build51-checker-fix.md)。以下保留发现问题时的证据。

## 结论与边界

《这是真的》第 4 章在 2026-09-16 21:57–22:02 CST 的两次写作停在 Checker。模型调用均正常返回；根因包含 Checker 未收到人物卡，误将已有关系身份当作新增，以及 `suspect` 终止整次写作的现行规则。其余线索、场景顺序等理由未逐条裁定，不能据此宣称两份稿件全部合格。本次只定位原因，未修改生产逻辑、正文、模型配置或触发生成。

## 必要证据

- Job `25ae2f34-ca0f-4561-8ba4-589416364460`：首次正文 3909 个去空白字符，触发长度重写；第二次 5265 字，通过确定性校验。Checker 返回 `suspect`，2 个 issue，终态 `checker_rejected`。
- Job `67a08e3d-80c9-474a-a8df-b23b97d8a600`：正文 5187 字，通过确定性校验。Checker 返回 `suspect`，4 个 issue，终态 `checker_rejected`。
- 两次 Selector、Writer、Checker 均使用 `deepseek-flash`，thinking 生效；7 次调用的结束原因为 `stop`，无上游错误码。当天两次灵感调用也正常返回。
- 人物卡在任务之前已明确记载相关两组恋爱关系；本次在服务器内核对，仅输出关系标签及更新时间，没有下载人物卡全文或失败候选。第二次检查中将男朋友身份定性为“新增关系”的部分与既有卡片冲突；附加位置、亲密细节等不能自动视为已获授权。
- 当前本章意图哈希与两个任务的输入哈希一致；章节仍为 `draft`，可见正文 0 字。失败候选按既定合同留在后端，没有提升为作者正文。
- 当前内外网鉴权 health 均 200／2.1.0；head `20260830_0013`，在途任务 0。服务 MainPID 与 `172.18.0.1:8787` 监听 PID 均为 567179，自动重启次数 0。
- 9 月 18 日 06:39 CST 有一次服务正常停止再启动，06:39:31 ready；时间晚于两次失败。9 月 15 日起 journal 中业务读取为 200，主要 404 来自根路径和不存在的探测路径，不是此次写作失败的错误码。

## 源码核对与后续

生产 `context.py`、`write_jobs.py`、`model_capabilities.py` 与本地 HEAD `d731880` 对应文件哈希一致。`writer_user_message` 提供人物卡、历史及上一章结尾；`checker_user_message` 只提供 Bible、姓名白名单和正文。`_run_job` 仅允许 `passed` 提升正文，`suspect` 与 `violation` 都进入拒绝分支。

修复方向：为 Checker 提供与 Writer 对齐的、已授权的既有事实，明确已有关系与本章新增事实的区别，再验证误判与真实越界两类用例。不能仅根据此次诊断放行历史失败稿，也不能直接移除检查闸门。

## 资源状态

排查脚本均通过标准输入运行；日志与数据库仅在服务器内只读处理，无本轮临时数据库、候选导出、日志下载或远端上传目录。仅保留本摘要作为必要证据；未清理既有发布物、真实数据或回退备份。
