# 2026-09-20 v2.1.0 Build 51 快修

## 目标与边界

- 用户要求把此前定位但未修复的问题一并修掉，在当前 Build 50 基础上增加一个 build。对外版本保持 2.1.0，双端 build 改为 51；包含此前通知可见性修复。
- 施工起点是 `d731880` 与已有未提交 Build 49/50 改动；完整保留原有工作，没有修改保护的 Xcode scheme，没有提交、推送、部署或换装。
- 原诊断见 `2026-09-20-checker-diagnosis.md`，通知修复见 `2026-09-20-notification-fix.md`。

## 修复决定

- 抽出共用既有事实段，自动写作开始前冻结，Writer 和 Checker 使用完全相同的世界观、允许人物、人物卡、章前投影状态、Selector 有来源记忆和上一章结尾。
- 手动复查从当前有效历史按既有相关性和字数预算提取资料，使用章前状态，不额外调用 Selector；不加载未来章节或未选人物卡，也不回灌全书末尾动态。
- 固定程序协议与检查指令明确：Bible 未重复说明既有关系不等于新增关系；历史和人物卡不能授权本章新增剧情、关系进展或白名单外人物。既有的证据字段与公开脱敏协议不变。
- 没有放宽 suspect/violation 的阻断，没有自动采用旧失败稿，也没有触发真实章节重写。

## 验证

- 针对性 19 项通过；后端完整回归 220 项通过、12 项按既有标记跳过（已退役的 v1.6 Extractor 在线协议）。Alembic head 仍为 `20260830_0013`，无迁移。
- 新回归覆盖生成时双角色事实快照一致、手动复查补齐资料、失败历史/未来状态隔离、passed/suspect/violation 的正文提升边界及拒绝原因脱敏。测试中的模型替身只证明传输与闸门，不冒充语义判断。
- 使用目标书当前模型与人格配置做两次独立的虚构短文检查，采用本轮新固定协议与提示，在服务器内存中执行、数据库开启 query_only，没有写入 JobRun 或真实内容：`deepseek-flash` 对已有男朋友身份返回 passed，对额外求婚订婚返回 violation；两次均正常 stop，总计 2543 tokens。
- 客户端状态门禁、iOS Debug 模拟器构建、iOS Release 真机签名构建、macOS Release 双架构签名构建通过；Mac 原产物及 ditto 副本、iOS 产物均通过 deep/strict 验签。真实书稿未重新请求模型，不据虚构用例保证所有长稿都能通过。
- Apple 支持检查保持只读，`pending_devices = [iPhone18,4]`：当前开发服务不可用，真机安装/运行尚未验证；需要连接解锁并由 Xcode 准备，未更改配对。

## 交付与下一步

- Mac：`/Users/linotsai/Lino/app_builds/ICTW-v2.1.0-build51-macOS.app`。
- iOS 签名产物留在当前 Xcode `Release-iphoneos/LinoI.app`，没有导出 IPA，最终安装由用户在 Xcode 操作。
- 详细验证与精确资源清单：`/Users/linotsai/Lino/app_builds/ICTW-v2.1.0-build51-validation.json`。
- 当前本机仍为 Build 49，生产 Backend 尚未应用本次 Checker 修复，公开 Release 仍是 Build 48。后续获发布授权后，先部署 Backend 并验证，再换装客户端；本地快修完成不等于线上已生效。

## 资源收尾

- 核对测试创建时间、目录名与本仓库用例归属，确认所有进程结束；对测试目录、日志目录及未安装且被替代的 Build 50 包生成逐项哈希/链接清单，lsof 检查无占用后复核删除。
- 共回收 26154843 字节，86 个文件/链接清单项；本轮临时测试库、日志与旧 Build 50 包剩余 0 字节，状态测试在系统临时目录无可执行文件残留。Build 50 的简短验证记录保留并标明已被替代。
- 保留 Build 51 Mac 安装包、iOS Xcode 签名产物与必要验证记录；共享 Xcode 缓存供后续安装/构建复用。已安装 Build 49 与既有恢复备份未动。远端仅通过标准输入执行只读配置读取和虚构测试，Python 使用 `-B`，无上传目录或临时文件。

## 一条龙发布（进行中）

- 用户已明确授权本次完整发布。累计核对范围：生产 Backend `65fafff` 到本次工作区；公开客户端 `v2.1.0-build48` 到 Build 51，且核对本机已安装 Build 49。生产所有 app Python 文件与 `65fafff` 无漂移。
- 累计变更包括 Build 49 本书模型设置、Build 50 通知可见性和 Build 51 Checker 上下文；此前本地全量回归及签名验证仍适用于未再修改的源码。本轮复验签名与发布物哈希。
- 发布健康接口使用配置中的 API 前缀，即 `/api/v1/health`；根路径 `/health` 的 404 不属于服务故障。
