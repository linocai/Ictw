# ICTW PROJECT_PLAN

> 唯一现行版本记录。这里只说明各版本完成了什么、当前发布状态和后续准备升级什么；施工细节、文件清单、接口字段、测试步骤与排障过程不写在这里。

## 当前基线

- 双端 App 源码为 `v2.0.4(46)`，Backend 源码与宁波生产均为 `v1.9.5(46)`；Alembic head 为 `20260814_0012`，本版无迁移。
- 本机 macOS 已安装并启动 `v2.0.4(46)`；iOS `v2.0.4(46)` 真机签名包已生成，安装由用户自行处理。
- `v1.9.5(46)` 已完成宁波生产部署与发布验收：内外网鉴权健康、单实例、数据库完整性、外键、零在途任务与公网门禁均通过，部署后 journal warning 为零；完整运维事实只记录在仓库外 `/Users/linotsai/Lino/NB_info.md`。
- 发布次序自本版起是硬约束：Backend 必须先于客户端换装，因为「删除这一章」在客户端只是乐观判断、真闸门在服务端。
- 香港旧服务保持 stopped + disabled，不参与现行生产。

## 当前版本方向

### 后续升级

- 审计中仍有两项待处理：迁移 downgrade 的级联删除（高危区）与章节列表的 O(N²) 查询，各需单独一轮。
- 发布流程的判据自 `v1.9.4(45)` 起有三处变化，已在生产实测确认：`/docs`、`/openapi.json`、`/redoc` 恒为 404，不再是健康判据；`/health` 会实际查库并比对期望 Alembic head，库不可用或结构落后一律 503；`.env` 中任何仍为 `change-me` 开头的密钥会导致后端拒绝启动，上产前应先做只读布尔检查。
- 搜索、标签、分卷、排序、推送和单书模型参数仍不在当前范围。

## 版本记录

### v2.0.4 / v1.9.5（Build 46）

- 恢复两个在 v2 clean-room 重写时丢失的章节能力。「重写本章」保留作者写给 Agent 的输入——本章意图、标题、出场人物、豁免名单与备注——只推倒 Agent 产出的正文与归档；「删除本章」连同作者输入一起抹掉整章。此前成稿章既无法重新生成也无法删除，唯一出路是手工清空正文。
- 「重写本章」不设最后一章限制，改为在确认框里列出会被作废归档的具体章号——因为重开后手改正文本来就能造成同样的级联，且一直不设防；「删除本章」只允许全书最后一章，由服务端强制。重写期间旧正文仍要等新稿双双通过校验才被替换，失败则原样保留。
- 一并修掉删除路径两处既有缺陷：注定被拒的请求不再先杀掉正在跑的生成；后续章节的在途写作输入现在会被真正作废，而不是靠"闸门下不可达"的论证搪塞——那条论证已被实测推翻。
- 两端入口由同一份共享状态与同一份确认文案驱动，iOS 阅读器与工作台的重复实现已合并；新增门禁守卫锁住入口的存在与接线，防止再被无声丢掉。同时修正 iOS 章节菜单缺少文字标签造成的可发现性问题——那正是本轮起因。
- 本版不改数据库结构，无迁移。已部署宁波生产并通过发布验收；macOS 已换装并启动，iOS 安装由用户自行处理。
- ⚠ 本版未做端到端手动验收：新增的是破坏性动作，在生产库上试错等于真丢数据，补验需要隔离后端与测试数据。

### v2.0.3 / v1.9.4（Build 45）

- 本版来自一轮覆盖 Backend、双端客户端、测试与文档的全量审计，内容全部是修复与收口，不新增任何产品能力，也不改动数据库结构。
- 客户端不再有"看起来成功、实际没保存"的路径：iOS 章节工作台与各面板现在都会显示保存失败；服务器回包不再覆盖往返期间输入的文字；打开某个 Agent 的人格页不会再清空它的推理配置；macOS 新增人物失败不再改写既有人物的固定设定，世界观与新建书保存失败也不再关闭面板丢弃输入。
- 归档口径收口：一次失败的归档尝试只作废它自己，不再连带剥夺章节自身的记忆来源；人物详情页与记忆导出统一为每章单一来源。
- 后端暴露面收口：关闭 CORS 通配与未鉴权可读的接口文档；`/health` 改为实际查库并比对本版期望的 Alembic head，使它能真正作为发布闸门；占位密钥拒绝启动；更换模型服务地址必须同时重填密钥；密钥无法解密、畸形地址和显式 null 由 500 改为可诊断的明确错误。
- 合同收口两项：`accept` 不再因为章节从未产生过候选就跳过确定性校验——未选／重名人物一律拒绝且不可忽略，长度不足可由作者显式忽略后接受；被拒候选的逐字引证不再随任务状态出网，只保留结论类型与理由，后端留档不变。两项均已写入铁律。
- 门禁本身也是本版的修复对象：客户端回归脚本此前在检索工具缺失时会把守卫判成"通过"，且指向已不再构建的旧文件；改为工具失败即失败并指向现行界面后，第一次真正跑完。另新增 ORM 与 Alembic head 的结构一致性守卫，实测当前无漂移。
- 双端 Release 构建、签名、ZIP 解包复核与 macOS 换装启动均已通过；macOS 改用标准 archive 流程出包，可执行体已 strip 并另存 dSYM。
- 本版已部署宁波生产并通过发布验收：零迁移，内外网鉴权健康返回 `1.9.4`，未鉴权 401，接口文档三个入口均已成为 404，单实例、数据库完整性、外键与零在途任务均通过，部署后 journal warning 为零；数据库内容未受部署影响。

### v2.0.2（Build 44）

- iOS 将恢复标准的层级返回与左缘右滑，避免自制页面手势抢占正文、输入和滚动操作；人物管理、世界观编辑、设置子页都保留清楚的返回、取消、保存和完成语义。
- 普通提示会自行淡出且可主动关闭，关键错误会持续显示并提供关闭入口；提示不再遮挡章节工作台和阅读器的底部动作。
- 人物改为可深入和返回的管理路径，世界观改为明确可取消／保存的编辑面，导出改为紧凑、可滚动、可取消准备的分享前流程；书级入口不会提供无效的“本章”范围。
- 修改中的持久化内容在离开前会明确处理，保存失败不会伪装成功或关闭编辑面；iOS 视觉与辅助功能统一安全边距、层级、控件状态和 Sheet 行为。
- 本版只提升 iOS target 至 `v2.0.2(44)`；macOS 继续为 `v2.0.1(43)`，Backend 不改动。
- 客户端状态、iOS Release、macOS 非功能回归及 Backend 既有门禁均已通过；iOS 真机签名包已生成并完成解包复核，安装由用户自行处理，Backend 未部署或重启。

### v2.0.1（Build 43）

- 以“创作工作台”和“成稿阅读器”为两条明确主路径，修复成稿章节仍像工作台、阅读动作实际创建新章节、桌面章节号换行等 v2.0.0 遗留体验问题。
- 不扩张内容生产或阅读功能：只让已有章节以真实顺序被阅读，让创建章节仅发生在作者明确选择全书末尾的新章动作时。
- 客户端状态与双端 Release 构建均已通过；macOS 已发布并换装，iOS 真机签名包已生成且由用户自行安装，Backend 未改动。

### v2.0.0（Build 41–42）

- 完成 iOS 与 macOS 的 clean-room 前端重写：统一书架、章节轨与工作台主路径，macOS 落地三栏工作台，iOS 落地紧凑分面；视觉与状态覆盖设计交接的既定范围。
- 以既有 `v1.9.3` Backend 合同接入真实写作、Checker、归档、人物、世界观、灵感、导出与人格能力，不为视觉稿改动服务端语义或增加迁移。
- 客户端状态、双端构建与隔离环境下的视觉验收均已通过；macOS 已发布并换装，iOS 安装由用户自行处理。
- Build 42 换用极简“终笔”双端图标，并在 macOS 工作台标题栏补齐与书架一致的 `ICTW` 品牌字标；产品合同与 Backend 均未改变。

### v1.9.3（Build 40）

- 完成 v2「台面」方向的合同纠偏：Writer 仍只在双重校验通过后更新当前正文，不提供候选全文、待采用新稿或采用／丢弃流程。
- 完成诚实的 Checker 结论状态：本地有效快照支持失效降级与可证明的正文差异，问题结论保留真实正文和 Bible 证据。
- 章节与书籍已提供归档健康汇总；失效归档预览仅供展示，与有效记忆、人物投影和后续写作严格隔离。
- 世界观保持单一长文本；人物固定设定与只读归档层分开呈现，章节计数仅基于可证明的归档记录。
- 新增共享正文导出，支持范围、纯文本、Markdown、逐章文件及可选的世界观与人物固定设定；记忆导出继续独立。
- 新增五个 Agent 的单书人格继承与覆盖，程序协议及模型配置仍全局强制；人格在任务启动时冻结，后续设置修改不影响在途任务。
- iOS 与 macOS 已完成最小语义接入；本地完整验证、迁移校验、双端构建和原型渲染验收均已通过。
- 本版已部署宁波 Backend 并迁移至新 head；生产数据完整性、鉴权健康和单实例门禁均通过。

### v1.9.2（Build 39）

- 灵感创造师的每条建议提升到 200–300 个去空白字符，形成连贯而不过度展开的章节进程。
- 新增一次性“本章推进边界”，用于约束人物关系、秘密、冲突和长期主线的推进速度，不写入 Bible 或数据库。
- 灵感创造师保持极简交互：明确触发、等待、结果或失败；不会因打开界面自动读取历史或请求模型。
- Extractor 与灵感创造师固定使用有界非思考模式，解决结构化输出耗尽额度和公网同步超时问题。
- Backend 已部署，本机 macOS 已换装；iOS 留给用户处理。

### v1.9.1（Build 37–38）

- 灵感创造师增加“承接历史”和“自由发想”两种内部模式，前几章、空 Bible 或无人选择时也能正常生成。
- 校验由整批失败改为逐条保留，只要有至少三条有效建议即可返回。
- 已有章节标题改为不可改拟的锚点，Agent 不再另拟标题；建议改用弹性场景思维形成自然连续的剧情方向。
- 修复入口无效、界面打开自动生成以及历史不足时反复完整性失败的问题。

### v1.9.0（Build 35–36）

- 新增完全独立的灵感创造师，可读取当前章节上下文和有效历史，每次给出 3–5 条可选择建议。
- 建议只加入本地 Bible 草稿，不进入 Writer、Checker 或 Extractor 流程，也不会自动保存。
- iOS 使用 Bible 就近入口，macOS 使用右侧灵感页；随后补充“只有明确点击按钮才生成”的交互边界。

### v1.8.3（Build 34）

- 完成 Writer generation 所有权、并发失效和终态事务收口，旧任务不能覆盖用户后续编辑。
- 完成 Extractor reopen、activation 和 recovery 生命周期修复，陈旧或不完整 revision 不能重新污染有效记忆。
- 统一 LLM 安全错误分类和配置错误响应，清除旧 Extractor correction 路径。

### v1.8.1（Build 32）

- 建立正文接受与归档解耦的 archive v2 流程；Extractor 失败不撤销已接受正文。
- 历史章节按 active v2 优先、合格 legacy 兜底的单一来源进入 Selector 和人物状态。
- 该版本仍是当前公开发布的双端客户端基线。

更早版本与完整验收记录见 `archive/plans/`。

## 后续版本 Backlog

- 搜索、标签、分卷、手动排序、推送和单书模型参数仍不在当前版本范围。

## 历史索引

- v2.0.4 执行计划：`docs/plans/v2.0.4-rewrite-and-delete-plan.md`
- v2.0.2 iOS 执行计划：`archive/plans/v2.0.2-ios-interaction-plan.md`
- v1.9.2 完整完成记录：`archive/plans/PROJECT_PLAN-v1.9.2-completed.md`
- v1.8.3 完整完成记录：`archive/plans/PROJECT_PLAN-v1.8.3-completed.md`
- v1.8.1 完整完成记录：`archive/plans/PROJECT_PLAN-v1.8.1-completed.md`
- 清理前完整规范：`archive/operations/AGENTS-through-2026-08-08.md`
